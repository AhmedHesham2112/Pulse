import Logout from "../features/authentication/logout";

function Header() {
  return (
    <header className="bg-white shadow p-[1.2rem 4.8rem]">
      <Logout />
    </header>
  );
}

export default Header;
